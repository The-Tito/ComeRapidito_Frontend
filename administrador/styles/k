.pasos {
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 30px;
  gap: 20px;
  width: 100%;
  max-width: 250px;
  margin-left: auto;
  margin-right: auto;
  
}
.pasos span {
  width: 50px;
  height: 50px;
  background-color: white;
  
  color: #ff3c00;
  border-radius: 50%;
  /* margin-top: 2px; /* Removed */
  font-size: 1.3rem;
  cursor: pointer;
  user-select: none;
  transition: background-color 0.3s ease, color 0.3s ease, transform 0.2s ease;
  box-shadow: 0 4px 6px rgba(0,0,0,0.2);
  border: 2px solid #ff3c00;
  line-height: 50px; /* Para ayudar al centrado vertical del texto */
  /* Combinamos los ajustes de traslación vertical y horizontal */
  transform: translate(1px, 1.5px); /* <-- Ajusta el primer valor (horizontal) para mover a la derecha */
}

.pasos span:hover {
  background-color: #ffe1d6;
  /* Combinamos scale y translate */
  transform: scale(1.05) translate(1px, 1.5px); /* <-- Aplica el mismo ajuste al hover */
}